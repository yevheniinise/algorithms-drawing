import React from 'react';
import { Alert } from 'react-bootstrap';

const Home = () => (
  <Alert variant="info">
    <Alert.Heading>Hey</Alert.Heading>
    <p>
      Pick an algorithm above you want to see drawing of
    </p>
  </Alert>
);

export default Home;
